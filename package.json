{
  "name": "qrcard-engine",
  "version": "1.0.0",
  "description": "Motor profesional de generación de tarjetas QR para WhatsApp",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "cli"
  ],

  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm start",
    "build": "npm run build:frontend && npm run build:backend && npm run build:cli",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm run build",
    "build:cli": "cd cli && npm run build",
    "start": "cd backend && npm start",
    "cli": "cd cli && node bin/qrcard.js",
    "test": "npm run test:backend && npm run test:frontend && npm run test:cli",
    "test:backend": "cd backend && npm test",
    "test:frontend": "cd frontend && npm test",
    "test:cli": "cd cli && npm test",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install && cd ../cli && npm install",
    "setup": "npm run install:all && npm run build"
  },

  "keywords": [
    "qr",
    "whatsapp",
    "business-card",
    "generator",
    "cli",
    "api"
  ],
  "author": "QRCard Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
